import React from 'react';

const ValidationComponent = props => {
    return(
    <div>
        <p>This is the number of letters:{props.name}</p> 
    </div>
    )
}

export default ValidationComponent;